{
  "name": "legal-platform",
  "private": true,
  "workspaces": [
    "apps/*"
  ],
  "scripts": {
    "dev": "concurrently -n case,auth,gateway -c blue,green,magenta \"npm run dev --workspace=apps/case-service\" \"npm run dev --workspace=apps/auth-service\" \"npm run dev --workspace=apps/gateway\"",
    "build": "npm run build --workspace=apps/case-service && npm run build --workspace=apps/auth-service && npm run build --workspace=apps/gateway",
    "start": "concurrently -n case,auth,gateway -c blue,green,magenta \"npm run start --workspace=apps/case-service\" \"npm run start --workspace=apps/auth-service\" \"npm run start --workspace=apps/gateway\"",
    "i-all": "npm install && npm install --workspace=apps/case-service && npm install --workspace=apps/auth-service",
    "tree": "node tree.js"
  },
  "dependencies": {
    "express": "^4.18.2"
  },
  "devDependencies": {
    "@types/express": "^4.17.21",
    "concurrently": "^8.2.0",
    "tsconfig-paths": "^4.2.0"
  }
}